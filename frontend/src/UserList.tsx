import React from 'react';

const UserList: React.FC = () => {
  return (
    <div>
      <h2>User List</h2>
      {/* TODO: Add user list rendering here */}
    </div>
  );
};

export default UserList;
